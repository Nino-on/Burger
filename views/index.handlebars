<h1>burgers</h1>

<h2>Lets make some good Burgers</h2>

<ul>
  {{#each BURGERS}}
    {{#unless this.Devoured}}
      <h5>{{this.id}}.{{this.Burger_Name}}</h5>
    {{/unless}}
  {{/each}}
</ul>

<h2>Those were good burgers</h2>

<ul>
  {{#each BURGERS}}
    {{#if this.Devoured}}
        <h5>{{this.id}}.{{this.Burger_Name}}</h5>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">What type of Burger is it:</label>
    <input type="text" id="ca" name="name">
  </div>

  

  <button type="submit">Add Burger</button>
</form>
